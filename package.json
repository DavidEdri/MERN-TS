{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "common:dev": "npm run dev --prefix packages/common",
    "server:dev": "npm run dev --prefix packages/server",
    "serve": "npm run start --prefix packages/server",
    "web": "npm run start --prefix packages/web",
    "dev": "concurrently \"npm run server:dev\" \"npm run common:dev\"",
    "build": "concurrently \"npm run build --prefix packages/server\" \"npm run build --prefix packages/common\"",
    "clean": "concurrently \"npm run clean --prefix packages/server \" \"npm run clean --prefix packages/common\"",
    "test": "npm run test --prefix packages/server"
  },
  "devDependencies": {
    "concurrently": "^5.0.0"
  },
  "name": "mern"
}
